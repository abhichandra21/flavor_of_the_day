# Runtime dependencies for Flavor of the Day integration
# These are the minimal dependencies needed for the integration to work

# Core integration dependencies (matches manifest.json)
aiohttp>=3.8.0
beautifulsoup4>=4.11.0
lxml>=4.9.0
python-dateutil>=2.8.0